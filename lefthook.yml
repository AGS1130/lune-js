pre-commit:
  parallel: true
  jobs:
    - name: oxlint
      run: bun lint

    - name: oxfmt-check
      run: bun fmt:check

    # ? Tests are passing and skipping (todo) correctly; not sure why it error exits
    # - name: test
    #   run: bun test
